typedef struct 
{ int* bytes,size,isNegative;  } bigInt;

void add(bigInt integer, int a)
{
    if(a>0 && isNegative)
    subtract(integer,-a);
    if(integer.bytes[0]+a)
    return;
}
void subtract(bigInt integer,int a)
{
    
}
int main()
{
    return 0;
}